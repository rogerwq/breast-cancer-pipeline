name        = "00 - Setup & Verify Models"
description = "Verify that all required CNN model files are present and loadable."

inputs  = []
outputs = ["model_status.json"]

depends_on = []

[container]
docker_image = "breast-cancer-pipeline:latest"

[scripts]
run = "run.sh"
